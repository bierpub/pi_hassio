  - platform: smtp
    name: emailjeff
    server: !secret email_hassio_server
    port: 587
    sender: !secret email_hassio_user
    username: !secret email_hassio_user
    password: !secret email_hassio_password
    starttls: True
    recipient: !secret email_jeff_recipient

  - platform: smtp
    name: emailrachel
    server: !secret email_hassio_server
    port: 587
    sender: !secret email_hassio_user
    username: !secret email_hassio_user
    password: !secret email_hassio_password
    starttls: True
    recipient: !secret email_rachel_recipient
    
  - platform: pushbullet
    name: mypushbullet
    api_key: !secret pushbullet_api

  - platform: html5
    name: html5
    vapid_pub_key: !secret vapid_pub_key
    vapid_prv_key: !secret vapid_prv_key
    vapid_email: !secret vapid_email

  - platform: pushover
    name: pushover    
    api_key: !secret pushover_api
    user_key: !secret pushover_user
